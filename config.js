var config = {
	debug : true,
	logLevel : 'info',	// log level (debug -> info -> error)

	documentURI : './resource/fail.html',	// Detect resource (You can use fail.html or pass.html to test this project)
	maxStrongTagCount : 15 // set strong tag max count (Default is 15)
};

module.exports = config;